# Parameter Decisions

| Parameter | Value | Date | Reason |
|---|---|---|---|
| SMT validity window | UNDECIDED | | |
| Zone tap expiry | UNDECIDED | | |
| Multiple SMT handling | Both streams (15m, 1m) qualify alone. Both present: 15m rules the stop; a 1m break does NOT invalidate (15m priority). 1m only: stop = extreme of the two SMT swings. 15m: stop = current swing leg (see strategy.md for the open payload-grounding point) | 2026-07-02 | Trader ruling from the discretionary model |
| Order type | UNDECIDED | | |
| Stop buffer | UNDECIDED | | |
| TP1 behavior | UNDECIDED | | |
| TP2 behavior | UNDECIDED | | |
| Risk percent | UNDECIDED | | |
| Max trades per day | UNDECIDED | | |
| Daily loss cutoff | UNDECIDED | | |
| Zone tap definition | Wick touch — bar range overlaps the zone band, evaluated at bar close | 2026-07-02 | Trader ruling; ICT "tagged" = any touch |
| Zone tap detection timeframe | 1-minute chart — PROPOSED, awaiting trader confirm | 2026-07-02 | Matches LTF SMT granularity; order-independence makes same-bar races harmless |
| Zone tap re-fire | Fire on each fresh entry into the band; server owns validity windows | 2026-07-02 | Trader delegated ("whichever is more efficient"); keeps all windowing logic in one place |
| Zone geometry source | Computed by self-authored Zone Publisher (4H chart). CALIBRATION PASSED 2026-07-05, FINAL config: PER-SLOT windows (60 same-clock-time candles = 60 trading days), dojis excluded, holiday-shortened included, percent-of-open, mean + STANDARD middle-pair median, manipulation = wick opposing close, distribution = EXPANSION FROM OPEN. Trader-verified 1:1 against Statmap across the full historical trail (all slots, both directions). Known display-only artifact: trail candles whose 60-day window crosses a contract roll can drift slightly on non-continuous charts — run the host on NQ1!; ages out naturally. Manual transcription REJECTED 2026-07-04 | 2026-07-05 | Per-slot proven by 2.5x adjacent-candle stat differences; every pooled-era hypothesis (full-range, sequence trim, lower-middle median, window offset) unwound |
| Zone calibration residual (accepted) | SUPERSEDED 2026-07-05: the residuals were artifacts of POOLED windowing. Statmap's historical tooltips proved PER-SLOT sampling (60 same-clock-time candles = 60 trading days): adjacent candles carry 2.5x different stats, impossible under pooling. Calibration REOPENED under per-slot; historical-trail comparison is the new verification surface | 2026-07-05 | The 2026-07-04 "impossibility proof" assumed pooled windows; per-slot dissolves it |
| Tap detection location | Server-side: heartbeat bars (1m Bar Reporter) checked against stored zone bands, wick-touch rule | 2026-07-04 | 1m chart lacks history for zone stats; splitting publisher/heartbeat keeps every stream automatic and puts tap logic in testable Python |
| Zone levels broadcast timing | Once per 4H candle at the OPEN (freq_once_per_bar) — documented exception to bar-close rule | 2026-07-04 | Levels are deterministic at open; server must hold geometry before the candle trades |
| Regular 4H bar durations (sample filter) | Slots 0–2, 4–5: 240 min; slot 3 (14:00 NY): 180 min — the daily 17:00 NY session halt truncates it | 2026-07-04 | Flat 240-min filter would exclude every 14:00 candle and kill zones for the secondary-setup window; holiday early closes still fail their own slot's expectation |
| SMT rebuild — detection core | LuxAlgo swing anatomy (fractal pivots, sign-product divergence test) with the PAIRING REPLACED (2026-07-05): every consecutive chart-swing pair is tested; the comparison side is its window extreme (±2 bars) around each swing bar — no comparison-pivot sync required, zero discards. Evaluation waits until the window is fully formed (reproduces the vendor's ~2-bar lag). Defaults: pivot lookback 1 (3-candle fractal), window 2. Two comparison symbols (ES on, YM off). LICENSE: divergence test adapted from © LuxAlgo, CC BY-NC-SA 4.0 — private non-commercial use only; never publish, sell, or redistribute | 2026-07-05 | LuxAlgo's same-bar sync + re-anchor rules discard by design (clean public charts); the model needs every adjacent swing pair judged |
| SMT rebuild — swing pivot strength | PROPOSED (swing mode only): left 2 / right 2 (fractal pivots). Right=2 matches the vendor's observed ~2-bar formation lag; left unobservable from payloads | 2026-07-05 | Calibrated via parallel run against the captured vendor stream (500+ formations with exact swing prices/times) |
| SMT rebuild — comparison swing matching | PROPOSED: comparison symbol's extreme within ±2 bars of the chart symbol's swing bar | 2026-07-05 | The two markets' swings rarely print the same bar; tolerance capped at pivot right-strength (later bars unconfirmed) |
| SMT rebuild — breakage rule | PROPOSED: price trading beyond the EXTREME of the two swings at bar close breaks the SMT (higher high for bearish / lower low for bullish) — deliberately strategy-aligned (extreme = stop level); the vendor's rule for flipped-orientation formations is unknown and the parallel run arbitrates | 2026-07-05 | Breaking the non-extreme swing must not invalidate a setup whose stop never traded (review finding) |
| SMT rebuild — equality rule | Trader-ruled 2026-07-05: EQUAL highs/lows COUNT as SMT when the other symbol breaks — divergence = break-disagreement (did each symbol strictly take out its previous swing; fire when they disagree). Equal-vs-equal and equal-vs-lower = agreement, no signal | 2026-07-05 | An equal high is a failure to break; one side breaking while the other fails is the definition of the divergence the model trades |
| SMT rebuild — active-list cap | 500 formed-unbroken SMTs (memory guard only, effectively unreachable); server-side staleness is governed by the still-UNDECIDED SMT validity window | 2026-07-05 | No-magic-numbers rule; silent eviction would desync publisher state from the server |
| SMT mark display filter (chart only) | Zone contact: mark draws when a leg touches the manipulation band OR the band was tapped within 30 minutes (= 2 fifteen-minute candles, trader ruling). Bands recomputed in-indicator with the calibrated zone formula; falls back to side-of-open where per-slot history is insufficient (1m charts). ALERTS ARE NEVER FILTERED | 2026-07-05 | Only zone-relevant SMTs deserve eyes; authoritative zone association stays server-side |
| IFVG rebuild — COMBINED SMT+IFVG engine | Trader-designed 2026-07-05: SMT layers (1m+15m) define the SMT LEG (window between the two swings); the CANDIDATE gap = the EXTREME 15s FVG on that leg (lowest down-gap for bullish, highest up-gap for bearish); RELEASE = 15s close back through the candidate. Origination is structural (no SMT leg → no candidate → no trigger). Leg slack 2 min, candidate max age 30 min, gap retention 2h. Stop lookback 40 bars, BESW = nearest 1m pivot (length 3) on profit side. Payloads carry gap_time_ny + smt_time_ny. Raw ote618-style TF streams (MPL 2.0, notice retained) optional, off by default | 2026-07-05 | The trigger IS the strategy's release: SMT leg's deepest imbalance, inverted. Replaces both the every-gap tracker and the ote618 single-recent-gap window as the primary engine |
| IFVG mark filter (display only) + MTF view | Marks draw only when the setup is ARMED (zone tapped within 30 min + matching SMT at/after the tap within 30 min) — a display approximation of the arm state; ALERTS NEVER FILTERED, server owns arming. Viewer instances (charts up to 15m) rebuild the 15s stream via security_lower_tf slices, marks only, never alert. MAINTENANCE DEBT: manip bands + SMT layers now duplicated in three indicators (Zone/SMT/IFVG Publishers) — any calibration change must touch all three | 2026-07-05 | Only sequence-live IFVGs deserve eyes (trader ruling); host/viewer split mirrors the Zone Publisher pattern |
| SMT HTF layers (in-indicator MTF) | 15m and 4H SMT layers detected via confirmed-close security swings and shown AT ALL TIMES on lower-TF charts (no zone filter — standing structure), heavier lines + TF-tagged labels; payload timeframe tokens "15 min(s)" / "240 min(s)". HTF pivot lookback 1. Layers self-disable when the chart TF is at/above them | 2026-07-05 | Trader ruling: H4/15m SMTs are the model's structure and must never be hidden; chart-TF marks stay zone-filtered for hygiene |
| Recreation-time in-progress tap | Report once on first realtime bar close after alert creation — PROPOSED, veto if unwanted | 2026-07-02 | Alert is re-created each 4H; a tap already in progress at creation otherwise never reaches the server, losing the tap AND the cycle's zone geometry |
